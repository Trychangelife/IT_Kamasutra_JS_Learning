function filter_list(l) {
    // Return a new array with the strings filtered out
    var arr = l.filter(element => typeof element === 'number');
        return arr;
  }



  for (let index = 0; index < array.length; index++) {
      const element = array[index];
      object
  }